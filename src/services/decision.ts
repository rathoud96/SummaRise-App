import firebase from 'firebase';

export class DecisionService{
    
    constructor(
    ) {}  
    
    check() {
        console.log("Checked!");
        // console.log(firebase.auth().currentUser);
        
    }
    
}